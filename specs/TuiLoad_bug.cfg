\* Mutation check: original design applied every response unconditionally.
\* Expect TLC to violate Coherent (a stale cross-view load clears the spinner).
CONSTANTS
    Views = {"dev", "rules"}
    MaxSeq = 3
    Guarded = FALSE
SPECIFICATION Spec
INVARIANTS
    TypeOK
    Coherent
