\* Mutation check: apply every response (the original naive design).
\* Expect Coherent to fail (a stale load overwrites current data).
CONSTANTS
    Views = {"dev", "rules"}
    MaxGen = 2
    Guard = "none"
SPECIFICATION Spec
INVARIANTS
    TypeOK
    Coherent
