\* Mutation check: naive "expiry removes any timed rule" MUST violate NoSever.
\* Expect TLC to report a counter-example (a stale timer severing a fresh pause).
CONSTANTS
    MaxId = 3
    Buggy = TRUE
SPECIFICATION Spec
INVARIANTS
    TypeOK
    NoSever
